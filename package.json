{
  "name": "hxchange",
  "version": "1.0.0",
  "description": "Hxchange root workspace",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && .venv\\Scripts\\activate && uvicorn main:app --reload\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
